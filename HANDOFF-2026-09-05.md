# Handoff — 2026-09-05

## What changed
- Added `CLAUDE.md` (developer guide: purpose, run/test, module layout, conventions, gotchas).
- `README.md` left as-is — already complete (title, why, privacy, develop, method, support, licence).

## Verified against the actual code
- `npm test` / `npm run serve` match `package.json` scripts exactly.
- Module list in CLAUDE.md matches `js/*.js` on disk (app, model, onboarding, charts, templates, cars, links).
- URL-hash share mechanism (`location.hash = "c=" + btoa(...)`) confirmed in `js/app.js`.
- `REFERRALS-TODO.md` confirmed present in `.gitignore` (private, not served).
- Fixed one inaccuracy: CLAUDE.md originally claimed `DEFAULT_RATES` *and* `CARS` are both
  `Object.freeze()`'d — only `DEFAULT_RATES` (in `model.js`) actually is; `CARS` (in `cars.js`) is a
  plain array. Corrected.

## Left to do
Nothing outstanding. No secrets, no invented features.
